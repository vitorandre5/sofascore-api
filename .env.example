PORT=8080
NODE_ENV=production
CORS_ORIGIN=*
API_PREFIX=/api
REQUEST_TIMEOUT=10000

# Optional variables already available in your Coolify server.
# Keep them if you later add cache, logs or persistence.
USER_SUPABASE=
PASSWD_SUPABASE=
